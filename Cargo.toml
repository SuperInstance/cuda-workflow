[package]
name = "cuda-workflow"
version = "0.1.0"
edition = "2021"
description = "DAG-based workflow orchestration with dependency resolution and parallel execution."
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
